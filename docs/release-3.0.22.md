# Stepsemble 3.0.22 release record

## Scope

Align the synthetic conversation catalog browser case with the collapsed Agent
Hub default by explicitly expanding the accessible preview before checking task
rows. Product behavior remains unchanged.

## Verification

- conversation catalog browser coverage expands Agent Hub before row assertions;
- full Node test suite and JavaScript checks;
- rolling browser compatibility across the release matrix.
