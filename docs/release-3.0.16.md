# Stepsemble 3.0.16 release record

## Scope

The primary Sessions list now merges Pi history with native and generic Agent
Hub task records. OpenCode conversations appear alongside Pi sessions with the
correct identity/logo, stable selection and pin keys, and the matching native
or generic open route.

## Verification

- cross-agent session-list helper tests;
- Agent Hub race tests and full smoke suite;
- JavaScript, version, and protocol checks;
- live Mac mini OpenCode adapter remains ready with 22 native sessions.
