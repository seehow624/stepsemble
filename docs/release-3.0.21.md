# Stepsemble 3.0.21 release record

## Scope

Align the synthetic Pi session browser case with the 3.0.19+ Sessions layout:
the Agent Hub preview is collapsed by default, so the case explicitly expands
the live panel before checking a stopped task row.

## Verification

- Pi session browser coverage expands the accessible Agent Hub disclosure;
- full Node test suite and JavaScript checks;
- rolling browser compatibility across the release matrix.
