# Compare generated client artifacts byte-for-byte on every OS, including Windows.
public/modules/*.js text eol=lf
client/protocol-types.d.ts text eol=lf
# Native Windows DDL uses CRLF; keep the canonical fixture LF and check both
# explicitly instead of allowing Git checkout configuration to change its pin.
protocol/native/codex/*.sql text eol=lf
